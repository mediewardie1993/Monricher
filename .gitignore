node_modules/
.next/
out/
*.log
Thumbs.db
.DS_Store

.vercel
*.rar
*.zip
*.7z
*.mkv
_video_frames/
tsconfig.tsbuildinfo

# Local MCP server config — holds live API tokens, never commit
.mcp.json

# Local secrets (GROQ_API_KEY, etc.) — set the real values in Vercel's
# project env vars instead, never commit them
.env
.env*.local
